#include "rectangle.h"
#include <stdio.h>

void calculate_rectangle_area(double length, double width) {
    double area = length * width;
    printf("Rectangle Area (Length 6, Width 3): %.2f\n", area);
}

